
apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  namespace: dev 
  labels:
    app: my-app
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080   
